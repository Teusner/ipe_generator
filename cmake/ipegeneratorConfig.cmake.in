# - Config file for the FooBar package
# It defines the following variables
#  IPEGENERATOR_INCLUDE_DIRS - include directories for FooBar
#  IPEGENERATOR_LIBRARIES    - libraries to link against
#  IPEGENERATOR_EXECUTABLE   - the bar executable

# Compute paths
get_filename_component(IPEGENERATOR_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(IPEGENERATOR_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# These are IMPORTED targets created by ipegeneratorTargets.cmake
set(IPEGENERATOR_LIBRARIES ipegenerator)
